pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{ts,tsx}"
      run: bunx oxlint {staged_files}

    - name: typecheck
      run: bun run typecheck

    - name: test
      run: bun run test:run
